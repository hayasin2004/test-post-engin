# 開発指示書
<!-- Claude Codeへ: このファイルを最初に読み、内容を理解してから開発を開始してください -->

## プロダクト概要
マルチペルソナ対応のX（Twitter）自動投稿システム。
Gemini APIでツイートを生成し、LINEで通知・承認後にX APIで投稿する。

## 役割
フルスタックエンジニア + システムアーキテクト + 自律AIエージェント

---

## 技術スタック（固定）
- Next.js 14+ / App Router / TypeScript strict
- DB: Supabase（PrismaなしでSQL直接実行）
- Deploy: Vercel
- CSS: CSS Modules（Tailwind禁止）
- 暗号化: Node.js built-in `crypto` モジュール（AES-256-GCM）

---

## 自律運用ルール

### 実行してよいこと（承認不要）
- Step内の実装・テスト・自己修復
- `npm run build` による自己検証（エラーは自己修復してから完了扱い）
- git commit（日本語Karmaスタイル: feat/fix/docs/refactor）
- PROGRESS.md のStep完了ごとの更新
- パッケージ追加

### 止まること（人間の判断が必要）
- `.env.local.example` 提示後 → 値の設定完了の返事を待つ
- 本番APIへの実際の呼び出し（決済・LINE送信・X投稿）
- 破壊的なSQL実行（DROP / TRUNCATE を含むもの）
- `.env.local` の内容をログ・出力に含める操作

### git commitルール
- 日本語Karmaスタイル（feat/fix/docs/refactor）
- Step完了ごとに必ずcommit

### 自己修復ルール
- 実装後は必ず `npm run build` で検証
- エラーはログを読んで自己修復してから完了扱い

---

## 環境変数ルール
- Step 1完了時に `.env.local.example` を出力して提示する
- 形式: `KEY=""  # 用途 | 取得先URL or コマンド`
- 命名: SCREAMING_SNAKE_CASE、サービス名プレフィックス
  （例: GEMINI_API_KEY, LINE_CHANNEL_SECRET, X_API_KEY）
- 値の設定は人間が行う。設定完了の返事が来るまで該当Stepを開始しない

---

## PROGRESS.md（最重要・引き継ぎの生命線）
<!-- 別AIが読んで即座に再開できる粒度で記載すること -->

以下フォーマットを厳守:

```
## プロダクト概要
マルチペルソナX自動投稿システム

## アーキテクチャ
- DB: Supabase | 理由: [1行] 
- UI: [ライブラリ名] | 理由: [1行] | 却下: [候補と理由]

## 実装済みファイルマップ
src/
├── app/
│   ├── api/
│   │   ├── generate/route.ts      # Gemini API ツイート生成
│   │   ├── line/webhook/route.ts  # LINE Webhook受信
│   │   └── post/route.ts          # X API 投稿実行
│   └── dashboard/page.tsx         # ダッシュボード
├── lib/
│   ├── crypto.ts                  # 暗号化/復号ユーティリティ
│   ├── supabase.ts                # Supabaseクライアント
│   ├── gemini.ts                  # Gemini APIクライアント
│   ├── line.ts                    # LINE APIクライアント
│   └── twitter.ts                 # X APIクライアント
supabase/
└── schema.sql                     # テーブル定義SQL

## Stepの進捗
- [ ] Step 1: 初期セットアップ
- [ ] Step 2: SupabaseテーブルSQL出力
- [ ] Step 3: Geminiツイート生成 + DB保存
- [ ] Step 4: LINE Webhook + 通知送信
- [ ] Step 5: X API投稿実行（暗号化復号）
- [ ] Step 6: ダッシュボードUI

## 環境変数
| キー名 | 用途 | 設定済み |
|--------|------|---------|
| ENCRYPTION_KEY | AES-256-GCM 暗号化マスターキー | - |
| SUPABASE_URL | SupabaseプロジェクトURL | - |
| SUPABASE_ANON_KEY | Supabase匿名キー | - |
| GEMINI_API_KEY | Gemini API | - |
| LINE_CHANNEL_ACCESS_TOKEN | LINE送信用 | - |
| LINE_CHANNEL_SECRET | LINE Webhook署名検証 | - |
| X_API_KEY | X API Key | - |
| X_API_SECRET | X API Secret | - |
| X_ACCESS_TOKEN | X Access Token（暗号化してDB保存） | - |
| X_ACCESS_SECRET | X Access Secret（暗号化してDB保存） | - |

## 未解決の課題
[問題の内容と試みた対処法]

## 次の着手内容
[次のStepの具体的なタスク]
```

---

## 開発ステップ

### Step 1: 初期セットアップ
- Next.js初期化・パッケージインストール
- `crypto.ts` の暗号化/復号ユーティリティ実装（AES-256-GCM）
- PROGRESS.md 作成
- `.env.local.example` を出力して提示 → **値の設定完了を待つ**
- `git commit: feat: プロジェクト初期化`

### Step 2: SupabaseテーブルSQL出力
以下3テーブルのCREATE文を `supabase/schema.sql` に出力する。
Prismaは使わず、手動実行用SQLのみ生成。

テーブル定義:
- `personas`: id, name, tone, topics(jsonb), x_credentials_encrypted, created_at
- `posts`: id, persona_id, content, status(generated/approved/posted/rejected), posted_at, created_at
- `settings`: id, key, value_encrypted, updated_at

- `git commit: feat: Supabaseスキー マSQL出力`

### Step 3: Geminiツイート生成 + DB保存
- `lib/gemini.ts`: `googleSearch` ツール有効化、過去投稿取得してネタ被り防止ロジック実装
- `app/api/generate/route.ts`: ペルソナごとに生成、postsテーブルへ保存
- ネタ被り防止ルール:
  - 技術的話題: 過去30件と類似度チェック（キーワード一致）
  - 日常的発言（おなかすいた/眠い等）: 重複許容
- `git commit: feat: Geminiツイート自動生成を実装`

### Step 4: LINE Webhook + 通知送信
- `app/api/line/webhook/route.ts`: 署名検証 → メッセージ解析 → 承認/却下処理
- `lib/line.ts`: 投稿案通知（ペルソナ名を明記したメッセージフォーマット）
  - 通知フォーマット例: `【ペルソナ名】\n{投稿内容}\n\n承認 or 却下`
- `git commit: feat: LINE Webhook通知を実装`

### Step 5: X API投稿実行
- `lib/twitter.ts`: `crypto.ts` で復号したX認証情報を使って投稿
- `app/api/post/route.ts`: LINE承認済みpostsをX APIで投稿、ステータス更新
- `lib/crypto.ts` 経由でのみ復号処理を行うこと（直接参照禁止）
- `git commit: feat: X API投稿実行を実装`

### Step 6: ダッシュボードUI
- `app/dashboard/page.tsx`: ペルソナ一覧・投稿履歴・生成トリガー
- CSS Modulesで実装（Tailwind禁止）
- `npm run build` / `npm run lint` 通過確認
- PROGRESS.mdを最終状態に更新
- `git commit: feat: ダッシュボードUIを実装`
- `git commit: docs: 最終状態をPROGRESS.mdに記録`

---

## コア要件・ビジネスロジック

### ターゲットユーザー
複数のX（Twitter）アカウント（ペルソナ）を運用する個人・チーム

### メイン機能
1. Gemini APIでペルソナごとにツイートを自動生成（googleSearch有効）
2. LINEで投稿案を通知し、承認/却下をメッセージで操作
3. 承認済みツイートをX APIで自動投稿

### セキュリティ要件
- X APIのAccess Token/SecretはAES-256-GCMで暗号化してDBに保存
- 暗号化/復号は必ず `lib/crypto.ts` 経由で行う
- ENCRYPTION_KEY は `.env.local` のみに存在し、DBには保存しない
- LINE Webhookは署名検証を必ず実施

### 制約
- SupabaseへのDB操作はPrismaを使わず `@supabase/supabase-js` を使用
- テーブル作成はSQL文を出力するのみ（自動実行しない）
- 本番APIの実際の呼び出しは人間の確認後に行う
