# 追加機能の実装要件：トレンド収集エンジンとツイート生成への統合

現在開発しているX自動投稿システムに対して、以下の「トレンド収集機能」を追加実装してください。
基本的な自律運用ルール（テスト実行、自己修復、KarmaスタイルでのGitコミット、PROGRESS.mdの更新）はこれまで通り厳守してください。

## 追加実装のステップ

### Step 1: データベースのアップデート
- 以下の構成で `daily_trends` テーブルを追加するためのSQLを出力してください（実行は私が手動で行います）。
- カラム構成: `id` (UUID), `date` (yyyy-mm-dd), `topic_category`, `content` (要約内容), `raw_data` (jsonb), `created_at`

### Step 2: トレンド収集API（Cronジョブ）の実装
- `app/api/cron/collect/route.ts` を作成してください（毎朝10:00実行を想定）。
- `lib/gemini.ts` などの既存ロジックを活用・拡張し、Gemini API（`googleSearch`有効）を使って以下の情報を収集・要約してください。
  1. IT/技術の最新ニュース
  2. 本日の株価・経済の動向
  3. XやTikTokでの現在の流行
  4. 愛知県名古屋市のローカルトピック
- 取得した要約情報を `daily_trends` テーブルに保存してください。
- 同時に、プロジェクトの `daily_trend/` フォルダ内に `yyyy_mm_dd.md` という名前で、誰が見ても分かりやすいマークダウン形式のトレンドレポートファイルを出力・保存する処理を追加してください。

### Step 3: 既存のツイート生成ロジックのアップデート
- 既存の `app/api/generate/route.ts` を改修してください。
- ツイートを生成する前に、新設した `daily_trends` テーブルから本日のトレンド情報を取得し、Geminiのプロンプトに「本日の参考情報」として注入するロジックを追加してください。

実装が完了し、`npm run build` でエラーが出ないことを確認したら、`PROGRESS.md` に新しいファイルマップや進捗を追記し、コミットして私に報告してください。